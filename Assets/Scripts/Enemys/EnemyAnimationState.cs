using UnityEngine;

/// <summary>
/// List of all Enemy Animation States, can be expanded later on 
/// </summary>
public enum EnemyAnimationState 
{
    Idle = 0,
    Run = 1,
    Attack = 2,
    Death = 3,
    ChargedWindup = 4,
    ChargedAttack = 5
}
